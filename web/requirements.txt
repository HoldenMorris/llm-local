# Pinned, because a container that rebuilds to a different framework version months later is a
# debugging session nobody asked for.
fastapi==0.115.6
uvicorn==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
